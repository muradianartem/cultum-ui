// Where users reach a person. Lives in lib/ rather than beside the Settings
// screens because api/feedback.js addresses mail to it too, and api/ should not
// import from screens/.

/** The monitored support inbox. */
export const SUPPORT_EMAIL = 'hello@cultum.app';
